@model IEnumerable<IMobile.Models.CartItemDevice>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cart)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Device)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cart.CartID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Device.Manufacture)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CartItemDeviceID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CartItemDeviceID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CartItemDeviceID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
